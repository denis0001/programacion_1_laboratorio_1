#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <ctype.h>
#include <string.h>
int main()
{
    char nombre[] = "FEDERICO";

    //char copia[9];
    strlwr(nombre);
    for (int i=0; nombre)

    }

    //strcpy(copia,nombre);
    printf("%s\n",nombre);
    //printf("%s", copia);
    return 0;
}
